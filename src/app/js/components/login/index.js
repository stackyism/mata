import React from 'react';
import  s from './Login.scss';
import withStyles from './../../decorators/withStyles';

const Login = (props) => (<div className="red">This is a login page!</div>);

export default withStyles(s)(Login);
